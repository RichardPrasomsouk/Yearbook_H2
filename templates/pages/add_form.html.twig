{% extends 'base.html.twig' %}

{% block body %}
    {{ form_start(form)}}
    <div class="row">
        <div class="col">{{ form_row(form.body) }}</div>
        <div class="col">{{ form_row(form.introduction) }}</div>
    </div>
    {{ form_rest(form) }}
{% endblock %}