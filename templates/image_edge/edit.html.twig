{% extends 'base.html.twig' %}

{% block title %}Edit ImageEdge{% endblock %}

{% block body %}
    <h1>Edit ImageEdge</h1>

    {{ include('image_edge/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_image_edge_index') }}">back to list</a>

    {{ include('image_edge/_delete_form.html.twig') }}
{% endblock %}
