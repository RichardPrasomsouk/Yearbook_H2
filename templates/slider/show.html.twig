{% extends 'base.html.twig' %}

{% block title %}Slider{% endblock %}

{% block body %}
    <h1>Slider</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ slider.id }}</td>
            </tr>
            <tr>
                <th>Pictures</th>
                <td>{{ slider.pictures }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_slider_index') }}">back to list</a>

    <a href="{{ path('app_slider_edit', {'id': slider.id}) }}">edit</a>

    {{ include('slider/_delete_form.html.twig') }}
{% endblock %}
