{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn__quote">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}


form_start --> <form action="" >

form_end --> </form>
