<form method="post" action="{{ path('app_paragraph_delete', {'id': paragraph.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ paragraph.id) }}">
    <button class="btn">Delete</button>
</form>
