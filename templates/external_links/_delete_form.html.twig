<form method="post" action="{{ path('app_external_links_delete', {'id': external_link.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ external_link.id) }}">
    <button class="btn">Delete</button>
</form>
